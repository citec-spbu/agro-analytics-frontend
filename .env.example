# Список origin родителя (хост), от которых принимается postMessage с JWT.
# Пусто = только для разработки (принимаются все origin). В проде задайте явно:
# VITE_PARENT_ORIGIN_ALLOWLIST=http://localhost:9000,https://app.example.com
VITE_PARENT_ORIGIN_ALLOWLIST=
